#pragma once

#include <vector>

using namespace std;

//This function gives back the position numbers of the compoenents
//of a vector v

std::vector<size_t> quicksortVec(std::vector<double> v);
